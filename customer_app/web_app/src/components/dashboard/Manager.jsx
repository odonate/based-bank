import React from 'react';

import styles from '@styles';

const Manager = ({}) => {
  return (
    <div className={styles.managerContainer}>
      Dashboard Page
    </div>
  );
};

export { Manager };
